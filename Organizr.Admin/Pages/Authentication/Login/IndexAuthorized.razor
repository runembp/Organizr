@attribute [Authorize]

@code {
    protected override async Task OnInitializedAsync()
    {
        NavigationManager.NavigateTo("/search-member", forceLoad:true);
        await base.OnInitializedAsync();
    }
}